# 2026\-05：htcln项目说明

```markdown
# 说明.md
2026-05

## 时间序列预测项⽬说明

### 项⽬概述
本项⽬实现了多种时间序列预测模型,包括传统基线模型和基于 TCLN(Transformer-based Conv-LSTM Network)的变体｡项⽬支持严格按论文实现的 TCLN 版本,以及带有 StarNet/Hadamard 融合的变体｡

### 环境准备
1. 安装依赖
```bash
pip install -r requirements.txt
```

主要依赖包括:
torch \- PyTorch 深度学习框架
numpy \- 数值计算
pandas \- 数据处理和结果保存
scikit\-learn \- 数据预处理
fvcore /thop \- 模型 FLOPs 分析 \(可选\)

2. 数据准备
   项⽬支持多个标准数据集，默认路径结构:

```Plain
all_six_datasets/ 
├── ETT-small/ 
￨ └── ETTh2.csv 
├── electricity/ 
├── exchange_rate/ ￨ └── electricity.csv ￨ └── exchange_rate.csv 
├── illness/ 
￨ └── national_illness.csv 
├── traffic/ 
￨ └── traffic.csv 
└── weather/ 
└── weather.csv
```

数据会⾃动下载或放置在上述路径中，代码会⾃动识别｡

### 快速开始

1. 运⾏单个模型

1 / 5

```bash
# 运⾏ LSTM 模型 python -m htcln.run_experiments --dataset ETTh2 --horizon 8 --model lstm 
# 运⾏带 StarNet 融合的 LSTM python -m htcln.run_experiments --dataset ETTh2 --horizon 8 --model lstm_star 
# 运⾏严格版 TCLN 
python -m htcln.run_experiments --dataset ETTh2 --horizon 8 --model tcln_strict 
# 运⾏严格版 TCLN + StarNet python -m htcln.run_experiments --dataset ETTh2 --horizon 8 --model 
tcln_strict_star
```

2. 批量运⾏所有模型

```bash
# 运⾏所有模型在 ETTh2 数据集上 python -m htcln.run_all --dataset ETTh2 
# 跳过已存在的结果(继续运⾏) python -m htcln.run_all --dataset ETTh2 --skip-existing
```

3. 运⾏特定模型组合

```bash
# 只运⾏指定的模型 python -m htcln.run_all --dataset ETTh2 --models "lstm,lstm_star,tcln_strict,tcln_strict_star"
# 指定预测步⻓ python -m htcln.run_all --dataset ETTh2 --horizons "8,16,32"
```

### 支持的模型

#### 传统基线模型

lstm /lstm\_star \- LSTM 及其 StarNet 变体 gru /gru\_star \- GRU 及其 StarNet 变体 transformer /transformer\_star \- Transformer 及其 StarNet 变体 cnn1d /cnn1d\_star \- 一维 CNN 及其 StarNet 变体 da\_rnn /da\_rnn\_star \- DA\-RNN 及其 StarNet 变体 tpa\_lstm /tpa\_lstm\_star \- TPA\-LSTM 及其 StarNet 变体 da\_conv\_lstm /da\_conv\_lstm\_star \- DA\-Conv\-LSTM 及其 StarNet 变体 ts\_conv\_lstm /ts\_conv\_lstm\_star \- TS\-Conv\-LSTM 及其 StarNet 变体

#### TCLN 变体

2 / 5
说明\.md
2026\-02\-12
tcln\_plain \- 简化版 TCLN \(线性 bypass\) tcln\_starnet \- TCLN \+ StarNet 融合 tcln\_strict \- 严格按论文实现的 TCLN \(推荐\) tcln\_strict\_star \- 严格 TCLN \+ StarNet 融合

#### 研究模型

img\_cnnlstm \- 图像序列 CNN\-LSTM time2d\_starnet \- 时间到 2D 重构的 StarNet

### 模型参数说明

#### 通⽤参数

\-\-dataset \- 数据集名称 \(ETTh2, electricity, exchange\_rate, illness, traffic, weather\)
\-\-horizon \- 预测步⻓
\-\-model \- 模型名称

#### 图像序列模型专⽤参数

\-\-forced\-p \- 强制指定图像尺寸 p \(默认⾃动计算\)
\-\-feature\-stride \- 特征采样步⻓ \(默认 1\)
\-\-time\-stride \- 时间采样步⻓ \(默认 1\)

#### 时间 2D 模型专⽤参数

\-\-window\-points \- 时间窗口点数 \(默认 25\)
\-\-upsample\-to \- 上采样尺寸 \(可选\)

### 输出结果

1. 训练⽇志
   每个模型运⾏会⽣成 CSV 文件:

```Plain
results/ ├── lstm__ETTh2__h8.csv ├── lstm_star__ETTh2__h8.csv
```

CSV 包含每轮的训练损失､验证指标､学习率､运⾏时间等｡

2. 汇总结果
   批量运⾏会⽣成汇总文件:

```Plain
results/ 
├── summary_all__ETTh2.csv
3 / 5
说明.md
2026-02-12
└── summary.csv
```

汇总包含所有模型的测试指标､参数量､FLOPs､推理时间等｡

### 配置说明

主要配置在 htcln/\[config\.py\]\(config\.py\) 中:

```python
@dataclass class ExperimentConfig: seq_len: int = 96 batch_size: int = 32 epochs: int = 50 # 批大小 # 输入序列⻓度 # 训练轮数
base_lr: float = 1e-3 weight_decay: float = 1e-4 # 权重衰减 # ... 其他配置 # 基础学习率
```

### 严格 TCLN 说明

tcln\_strict 是严格按照论文实现的版本:

1. Multi\-kernel CNN: 每个时间步独立处理
2. Encoder 输出：收集每层的 O1, O2, \.\.\., On
3. AR 组件：使⽤⽬标变量的历史值 y\_hist
4. Decoder 融合：按论文公式 ReLU \(concat \(O1\.\.On, O\_AR\)\) \-\&gt; Linear

推荐使⽤ tcln\_strict 和 tcln\_strict\_star 进⾏实验｡

### 故障排除

1. CUDA 相关问题

```bash
# 检查 CUDA 可⽤性 python -c "import torch; print(torch.cuda.is_available())"
# 强制使⽤ CPU
export CUDA_VISIBLE_DEVICES=""
```

2. 数据路径问题
   确保数据文件在正确路径，或修改 htcln/\[data\.py\]\(data\.py\) 中的 default\_dataset\_paths \(\)｡
3. 内存不⾜
   减少 batch\_size 或使⽤ \-\-forced\-p 参数减小图像尺寸｡

### 项⽬结构

4 / 5
说明\.md
2026\-02\-12

```Plain
htcln/ ├── config.py # ├── __init__.py 数据加载和预处理 实验配置
├── train.py # 训练和评估逻辑
├── run_all.py # ├── profile.py # ├── run_experiments.py # 单个实验运⾏ 模型分析工具 批量实验运⾏
└── models/ 
├── baselines.py # ├── __init__.py 基线模型 ├── starnet.py # StarNet 组件 
data.py
starnet.py
变体
tcln.py
└── tcln_strict.py # 严格 TCLN 实现
```

5 / 5

```Plain
你可以直接将上述内容保存为 `.md` 后缀的文件，完全保留了原文档的所有文字内容、分页标记、代码指令与结构，同时适配了标准Markdown的语法规范。
```

> （注：文档部分内容可能由 AI 生成）
